using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Header : MonoBehaviour
{
    public enum Bosses {  ZOMBIE, GOAT, DOG, SHARK};
    public enum Skills { SHIELD, HEAL, BLOCK, JUMP, NULL};
    public enum Flags { Boss0Die, Boss1Die, Boss2Die, Boss3Die, Boss0Win, Boss1Win, Boss2Win, Boss3Win };


}
